<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Madina Beauté — Luxury Beauty Studio, Las Vegas</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,400;0,500;0,600;1,400;1,500&family=Jost:wght@300;400;500;600&display=swap" rel="stylesheet">
<style>
:root {
    --ink: #0c0908;
    --ink-2: #16100e;
    --cream: #faf6f0;
    --cream-dark: #f0e6da;
    --rose: #d4a097;
    --gold: #c59b57;
    --gold-soft: #e5cb9e;
    --wine: #5a1c29;
    --emerald: #144231;
    --emerald-light: #2a634e;
    --charcoal: #261f1c;
    --font-display: 'Cormorant Garamond', serif;
    --font-base: 'Jost', sans-serif;
    --transition: cubic-bezier(0.16, 1, 0.3, 1);
  }

  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }

  html {
    scroll-behavior: smooth;
    background: var(--cream);
  }

  body {
    font-family: var(--font-base);
    color: var(--charcoal);
    background: var(--cream);
    overflow-x: hidden;
    -webkit-font-smoothing: antialiased;
  }

  ::selection {
    background-color: var(--emerald);
    color: var(--cream);
  }

  .container {
    max-width: 1240px;
    margin: 0 auto;
    padding: 0 40px;
  }

  a { text-decoration: none; color: inherit; transition: color 0.4s var(--transition); }
  ul { list-style: none; }

  @media (prefers-reduced-motion: reduce){
    *{animation-duration:0.01ms !important; animation-iteration-count:1 !important; transition-duration:0.01ms !important; scroll-behavior:auto !important;}
  }

  /* Сглаженный утонченный Hover Shake взамен грубого */
  @keyframes subtleShake {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-2px); }
  }
  .hover-shake { transition: transform 0.4s var(--transition); }
  .hover-shake:hover { 
    animation: subtleShake 0.6s var(--transition); 
  }
  .hover-shake:not(.btn):hover {
    color: var(--gold) !important;
  }

  .reveal {
    opacity: 1;
    transform: none;
  }

  /* ---------- HEADER ---------- */
  .header {
    position: fixed;
    top: 0; left: 0; right: 0;
    z-index: 100;
    background: transparent;
    transition: all 0.6s var(--transition);
    padding: 32px 0;
  }
  .header.scrolled {
    background: rgba(12, 9, 8, 0.88);
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
    padding: 16px 0;
    box-shadow: 0 4px 30px rgba(0, 0, 0, 0.08);
    border-bottom: 1px solid rgba(229, 203, 158, 0.15);
  }
  .nav-container {
    display: flex;
    align-items: center;
    justify-content: space-between;
  }
  .logo {
    font-family: var(--font-display);
    font-size: 2rem;
    font-weight: 500;
    letter-spacing: .02em;
    color: var(--cream);
    line-height: 0.9;
    display: flex;
    flex-direction: column;
  }
  .logo span {
    font-family: var(--font-base);
    font-size: .6rem;
    letter-spacing: .45em;
    text-transform: uppercase;
    color: var(--gold-soft);
    margin-top: 6px;
    font-weight: 400;
  }
  .nav-menu {
    display: flex;
    gap: 48px;
  }
  .nav-link {
    position: relative;
    color: var(--cream);
    font-size: .85rem;
    letter-spacing: .2em;
    text-transform: uppercase;
    font-weight: 400;
    padding-bottom: 6px;
    opacity: 0.85;
    transition: opacity 0.4s var(--transition);
  }
  .nav-link:hover { opacity: 1; }
  .nav-link::after {
    content: '';
    position: absolute;
    left: 50%; bottom: 0;
    width: 0; height: 1px;
    background: var(--gold-soft);
    transition: all 0.4s var(--transition);
    transform: translateX(-50%);
  }
  .nav-link:hover::after { width: 100%; }

  .burger {
    display: none;
    flex-direction: column;
    justify-content: space-between;
    width: 28px; height: 16px;
    background: none; border: none;
    cursor: pointer; z-index: 110;
  }
  .burger span {
    display: block; height: 1px; width: 100%;
    background: var(--cream);
    transition: all 0.4s var(--transition);
  }
  .burger.active span:nth-child(1) { transform: translateY(7.5px) rotate(45deg); }
  .burger.active span:nth-child(2) { opacity: 0; transform: translateX(-10px); }
  .burger.active span:nth-child(3) { transform: translateY(-7.5px) rotate(-45deg); }

  /* ---------- HERO ---------- */
  .hero {
    position: relative;
    min-height: 100vh;
    display: flex;
    align-items: center;
    background:
      radial-gradient(circle 800px at 85% 20%, rgba(197, 155, 87, 0.15), transparent 80%),
      radial-gradient(circle 900px at 15% 85%, rgba(20, 66, 49, 0.4), transparent 75%),
      linear-gradient(135deg, var(--ink) 0%, var(--ink-2) 100%);
    overflow: hidden;
  }
  .hero::before {
    content: '';
    position: absolute; inset: 0;
    background-image: 
      linear-gradient(rgba(215, 181, 121, 0.015) 1px, transparent 1px),
      linear-gradient(90deg, rgba(215, 181, 121, 0.015) 1px, transparent 1px);
    background-size: 100px 100px;
    pointer-events: none;
  }
  .hero-content {
    position: relative;
    max-width: 750px;
    padding-top: 80px;
  }
  .hero-subtitle {
    font-size: .8rem;
    letter-spacing: .45em;
    text-transform: uppercase;
    color: var(--gold-soft);
    margin-bottom: 24px;
    font-weight: 500;
  }
  .hero-title {
    font-family: var(--font-display);
    color: var(--cream);
    font-weight: 400;
    font-size: clamp(3.2rem, 7vw, 5.8rem);
    line-height: 1.05;
    letter-spacing: -0.01em;
  }
  .hero-title i {
    color: var(--rose);
    font-style: italic;
    font-weight: 300;
    position: relative;
  }
  .hero-desc {
    margin-top: 32px;
    max-width: 520px;
    font-size: 1.1rem;
    line-height: 1.8;
    font-weight: 300;
    color: rgba(250, 246, 240, 0.75);
  }
  
  .btn {
    display: inline-block;
    margin-top: 48px;
    padding: 18px 44px;
    border: 1px solid rgba(197, 155, 87, 0.6);
    color: var(--cream);
    font-size: .8rem;
    letter-spacing: .25em;
    text-transform: uppercase;
    position: relative;
    z-index: 1;
    background: transparent;
    transition: all 0.5s var(--transition);
  }
  .btn::before {
    content: '';
    position: absolute; inset: 0;
    background: linear-gradient(90deg, var(--gold) 0%, var(--gold-soft) 100%);
    transform: scaleY(0);
    transform-origin: bottom;
    transition: transform 0.5s var(--transition);
    z-index: -1;
  }
  .btn:hover {
    color: var(--ink);
    border-color: var(--gold-soft);
    box-shadow: 0 10px 30px rgba(197, 155, 87, 0.2);
    transform: translateY(-2px);
  }
  .btn:hover::before { transform: scaleY(1); }

  .scroll-cue {
    position: absolute;
    bottom: 40px; left: 40px;
    display: flex;
    align-items: center;
    gap: 16px;
    color: rgba(250, 246, 240, 0.4);
    font-size: .75rem;
    letter-spacing: .3em;
    text-transform: uppercase;
  }
  .scroll-cue .line {
    width: 48px; height: 1px;
    background: rgba(250, 246, 240, 0.2);
    position: relative; overflow: hidden;
  }
  .scroll-cue .line::after {
    content: '';
    position: absolute; left: -48px; top: 0;
    width: 48px; height: 1px;
    background: var(--gold-soft);
    animation: slide 2.5s cubic-bezier(0.785, 0.135, 0.15, 0.86) infinite;
  }
  @keyframes slide {
    0% { left: -48px; }
    40%, 100% { left: 100%; }
  }

  /* ---------- ANNOUNCEMENT BAR ---------- */
  .announce-bar {
    background: var(--emerald);
    color: var(--cream);
    text-align: center;
    padding: 12px 20px;
    font-size: .76rem;
    letter-spacing: .16em;
    text-transform: uppercase;
    position: relative;
    z-index: 101;
    border-bottom: 1px solid rgba(250, 246, 240, 0.1);
  }
  .announce-bar strong { color: var(--gold-soft); font-weight: 500; }

  /* ---------- MARQUEE ---------- */
  .marquee {
    background: #140f0d;
    overflow: hidden;
    white-space: nowrap;
    padding: 24px 0;
    border-top: 1px solid rgba(197, 155, 87, 0.15);
    border-bottom: 1px solid rgba(197, 155, 87, 0.15);
  }
  .marquee-track {
    display: inline-flex;
    animation: marquee 40s linear infinite;
  }
  .marquee span {
    font-family: var(--font-display);
    font-size: 1.4rem;
    color: var(--cream);
    opacity: 0.8;
    padding: 0 40px;
    letter-spacing: 0.05em;
  }
  .marquee span::after {
    content: '✦';
    margin-left: 40px;
    color: var(--gold);
    font-size: 0.8rem;
  }
  @keyframes marquee{
    from{transform:translateX(0);}
    to{transform:translateX(-50%);}
  }

  /* ---------- SECTIONS SHARED ---------- */
  .section {
    padding: 140px 0;
    scroll-margin-top: 100px;
  }
  .eyebrow {
    display: block;
    font-size: .8rem;
    letter-spacing: .4em;
    text-transform: uppercase;
    color: var(--emerald-light);
    margin-bottom: 20px;
    font-weight: 600;
  }
  .section-title {
    font-family: var(--font-display);
    font-size: clamp(2.4rem, 5vw, 3.6rem);
    font-weight: 400;
    color: var(--charcoal);
    max-width: 700px;
    line-height: 1.15;
  }
  .section-head { margin-bottom: 80px; }

  /* ---------- SERVICE FILTERS ---------- */
  .filter-bar {
    display: flex;
    gap: 16px;
    flex-wrap: wrap;
    margin-bottom: 64px;
  }
  .filter-btn {
    padding: 12px 28px;
    border: 1px solid rgba(38, 31, 28, 0.12);
    background: transparent;
    font-family: var(--font-base);
    font-size: .75rem;
    letter-spacing: .2em;
    text-transform: uppercase;
    color: var(--charcoal);
    cursor: pointer;
    transition: all 0.4s var(--transition);
  }
  .filter-btn:hover { 
    border-color: var(--emerald); 
    color: var(--emerald);
  }
  .filter-btn.active {
    background: var(--emerald);
    border-color: var(--emerald);
    color: var(--cream);
    box-shadow: 0 8px 20px rgba(20, 66, 49, 0.2);
  }
  .service-row{display:none;}
  .service-row.show{display:grid;}

  /* ---------- SERVICES ---------- */
  .service-list { display: flex; flex-direction: column; }
  .service-row {
    display: grid;
    grid-template-columns: 120px 1.2fr 2fr;
    gap: 40px;
    align-items: center;
    padding: 48px 0;
    border-top: 1px solid rgba(38, 31, 28, 0.08);
    transition: all 0.5s var(--transition);
    position: relative;
  }
  .service-list .service-row:last-child {
    border-bottom: 1px solid rgba(38, 31, 28, 0.08);
  }
  .service-row::before {
    content: '';
    position: absolute; inset: 0;
    background: linear-gradient(90deg, rgba(20, 66, 49, 0.03) 0%, transparent 100%);
    opacity: 0; transition: opacity 0.5s var(--transition);
    pointer-events: none;
    z-index: -1;
  }
  .service-row:hover {
    padding-left: 24px;
  }
  .service-row:hover::before { opacity: 1; }
  .service-row:hover .service-mark { transform: scale(1.05); }
  .service-row:hover .card-title { color: var(--emerald); }

  .service-mark {
    font-family: var(--font-display);
    font-style: italic;
    font-size: 3.6rem;
    line-height: 1;
    opacity: 0.85;
    transition: all 0.5s var(--transition);
  }
  /* Дорогая мягкая кастомизация цветов по категориям */
  .service-row[data-cat="skin"] .service-mark { color: var(--wine); }
  .service-row[data-cat="pmu"] .service-mark { color: var(--rose); }
  .service-row[data-cat="lashes"] .service-mark { color: var(--gold); }
  .service-row[data-cat="nails"] .service-mark { color: var(--emerald); }

  .card-title {
    font-family: var(--font-display);
    font-size: 1.8rem;
    font-weight: 400;
    color: var(--charcoal);
    transition: color 0.4s var(--transition);
  }
  .card-desc {
    font-size: 1rem;
    line-height: 1.8;
    font-weight: 300;
    color: rgba(38, 31, 28, 0.7);
  }

  /* ---------- BEYOND THE CHAIR ---------- */
  .beyond-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 32px;
    background: transparent;
  }
  .beyond-item {
    background: #ffffff;
    padding: 56px 44px;
    border: 1px solid rgba(38, 31, 28, 0.05);
    box-shadow: 0 4px 30px rgba(0, 0, 0, 0.01);
    transition: all 0.6s var(--transition);
  }
  .beyond-item:hover {
    background: var(--ink);
    border-color: var(--ink);
    transform: translateY(-8px);
    box-shadow: 0 20px 40px rgba(17, 12, 10, 0.15);
  }
  .beyond-item:hover h3 { color: var(--gold-soft); }
  .beyond-item:hover p { color: rgba(250, 246, 240, 0.65); }
  
  .beyond-item h3 {
    font-family: var(--font-display);
    font-style: normal;
    font-weight: 400;
    font-size: 1.7rem;
    color: var(--charcoal);
    margin-bottom: 20px;
    transition: color 0.4s var(--transition);
  }
  .beyond-item p {
    font-size: .98rem;
    line-height: 1.8;
    font-weight: 300;
    color: rgba(38, 31, 28, 0.65);
    transition: color 0.4s var(--transition);
  }
  .beyond-cta { margin-top: 72px; text-align: center; }

  /* ---------- GALLERY ---------- */
  .gallery-grid{
    display:grid;
    grid-template-columns:repeat(6,1fr);
    grid-auto-rows:140px;
    gap:20px;
  }
  .gallery-tile{
    position:relative;
    grid-column:span 2;
    grid-row:span 2;
    overflow:hidden;
    display:flex;
    align-items:flex-end;
    padding:32px;
    color:var(--cream);
    box-shadow: 0 10px 40px rgba(0,0,0,0.03);
    transition:all .6s var(--transition);
  }
  .gallery-tile:nth-child(1){grid-column:span 3; grid-row:span 3;}
  .gallery-tile:nth-child(2){grid-column:span 3; grid-row:span 2;}
  .gallery-tile:nth-child(3){grid-column:span 3; grid-row:span 2;}
  .gallery-tile:nth-child(4){grid-column:span 2; grid-row:span 2;}
  .gallery-tile:nth-child(5){grid-column:span 2; grid-row:span 2;}
  .gallery-tile:nth-child(6){grid-column:span 2; grid-row:span 2;}
  
  .gallery-tile::before{
    content:'';
    position:absolute; inset:0;
    z-index:0;
    opacity: 0.92;
    transition: opacity 0.4s var(--transition);
  }
  /* Дорогой мерцающий свет в галерее вместо грубых плашек */
  .gallery-tile:nth-child(1)::before{background:radial-gradient(circle at top left, var(--wine), #000);}
  .gallery-tile:nth-child(2)::before{background:radial-gradient(circle at top left, var(--emerald), #000);}
  .gallery-tile:nth-child(3)::before{background:radial-gradient(circle at top left, var(--gold), #000);}
  .gallery-tile:nth-child(4)::before{background:radial-gradient(circle at top left, var(--ink-2), var(--wine));}
  .gallery-tile:nth-child(5)::before{background:radial-gradient(circle at top left, var(--emerald-light), #000);}
  .gallery-tile:nth-child(6)::before{background:radial-gradient(circle at top left, var(--charcoal), var(--rose));}
  
  .gallery-tile:hover{
    transform: translateY(-4px);
    box-shadow: 0 20px 40px rgba(0,0,0,0.12);
  }
  .gallery-tile:hover::before {
    opacity: 1;
  }
  .gallery-label{
    position:relative; z-index:1;
    font-family:var(--font-display);
    font-style:italic;
    font-size:1.4rem;
    letter-spacing: 0.02em;
  }

  /* ---------- CONCEPT (ABOUT) ---------- */
  .concept-bg {
    background: #0c0908;
    color: var(--cream);
    position: relative;
  }
  .concept-bg::after {
    content: ''; position: absolute; inset: 0;
    background: radial-gradient(circle 700px at 50% 50%, rgba(20, 66, 49, 0.15), transparent 80%);
    pointer-events: none;
  }
  .concept-bg .eyebrow { color: var(--gold-soft); }
  .concept-bg .section-title { color: var(--cream); }
  
  .concept-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 48px;
  }
  .concept-item {
    padding: 16px 0 16px 40px;
    border-left: 1px solid rgba(250, 246, 240, 0.1);
    transition: border-color 0.4s var(--transition);
  }
  .concept-item:first-child { padding-left: 0; border-left: none; }
  .concept-item:hover { border-color: var(--emerald-light); }
  
  .concept-item h3 {
    font-family: var(--font-display);
    font-weight: 400;
    font-size: 1.8rem;
    color: var(--gold-soft);
    margin-bottom: 20px;
  }
  .concept-item p {
    font-size: 1rem;
    line-height: 1.85;
    font-weight: 300;
    color: rgba(250, 246, 240, 0.65);
  }

  /* ---------- CONTACT & FORM ---------- */
  .contacts-wrapper {
    display: grid;
    grid-template-columns: 1fr 1.2fr;
    gap: 80px;
    align-items: flex-start;
  }
  .contact-item {
    padding: 32px 0;
    border-top: 1px solid rgba(38, 31, 28, 0.08);
  }
  .contact-item:first-child { border-top: none; padding-top: 0; }
  .contact-item h4 {
    font-size: .75rem;
    letter-spacing: .25em;
    text-transform: uppercase;
    color: var(--emerald-light);
    margin-bottom: 12px;
    font-weight: 600;
  }
  .contact-item p {
    font-family: var(--font-display);
    font-size: 1.6rem;
    font-weight: 400;
  }
  .contact-item a:hover { color: var(--emerald); }

  .instagram-btn { border-color: var(--charcoal); color: var(--charcoal); }
  .instagram-btn::before { background: var(--charcoal); }
  .instagram-btn:hover { color: var(--cream); border-color: var(--charcoal); }
  
  .whatsapp-btn { border-color: #2da15f; color: #2da15f; margin-left: 20px; }
  .whatsapp-btn::before { background: #2da15f; }
  .whatsapp-btn:hover { color: var(--cream); border-color: #2da15f; }

  .contact-form{
    display:flex;
    flex-direction:column;
    gap:28px;
    background: #ffffff;
    padding: 48px;
    border: 1px solid rgba(38, 31, 28, 0.05);
    box-shadow: 0 15px 50px rgba(0,0,0,0.02);
  }
  .form-row{
    display:grid;
    grid-template-columns:1fr 1fr;
    gap:24px;
  }
  .form-field{
    display:flex;
    flex-direction:column;
    gap:8px;
  }
  .form-field label{
    font-size:.7rem;
    letter-spacing:.2em;
    text-transform:uppercase;
    color: var(--charcoal);
    opacity: 0.6;
    font-weight:600;
  }
  .form-field input,
  .form-field select,
  .form-field textarea{
    font-family:var(--font-base);
    font-size:.95rem;
    color:var(--charcoal);
    background:transparent;
    border:none;
    border-bottom:1px solid rgba(38,31,28,.15);
    padding:12px 2px;
    transition:border-color .4s var(--transition);
  }
  .form-field input:focus,
  .form-field select:focus,
  .form-field textarea:focus{
    outline:none;
    border-color:var(--emerald);
  }
  .form-field textarea{resize:vertical; min-height:100px;}
  .form-submit{
    align-self:flex-start;
    margin-top:12px;
    cursor:pointer;
    font-family:var(--font-base);
    border-color: var(--emerald);
    color: var(--emerald);
  }
  .form-submit::before {
    background: var(--emerald);
  }
  .form-submit:hover {
    color: var(--cream);
    border-color: var(--emerald);
  }
  .form-note{
    font-size:.82rem;
    color:rgba(38,31,28,.55);
    font-weight:300;
    margin-top: -4px;
  }

  .map-box {
    margin-top: 40px;
    border: none;
    box-shadow: 0 20px 50px rgba(0,0,0,0.05);
    filter: grayscale(1) contrast(1.02) brightness(0.98);
    transition: filter 0.8s var(--transition);
  }
  .map-box:hover { filter: grayscale(0.1) contrast(1); }
  .map-box iframe{display:block;}

  /* ---------- FOOTER ---------- */
  .footer {
    background: #060505;
    color: var(--cream);
    padding: 88px 0 40px;
    text-align: center;
    border-top: 1px solid rgba(250, 246, 240, 0.05);
  }
  .footer-logo {
    font-family: var(--font-display);
    font-size: 1.8rem;
    letter-spacing: .15em;
    margin-bottom: 16px;
    color: var(--cream);
  }
  .footer-tagline {
    font-size: .75rem;
    letter-spacing: .25em;
    text-transform: uppercase;
    color: var(--gold-soft);
    margin-bottom: 48px;
    opacity: 0.8;
  }
  .copyright {
    font-size: .8rem;
    color: rgba(250, 246, 240, 0.3);
    font-weight: 300;
    border-top: 1px solid rgba(250, 246, 240, 0.05);
    padding-top: 32px;
  }

  /* ---------- RESPONSIVE GRID FIXED ---------- */
  @media (max-width: 1024px) {
    .beyond-grid { grid-template-columns: 1fr; gap: 24px; }
    .concept-grid { grid-template-columns: 1fr; gap: 32px; }
    .concept-item { border-left: none; padding: 32px 0 0 0; border-top: 1px solid rgba(250, 246, 240, 0.1); }
    .concept-item:first-child { border-top: none; padding-top: 0; }
    .contacts-wrapper { grid-template-columns: 1fr; gap: 56px; }
    .gallery-grid { grid-template-columns: repeat(4, 1fr); }
    .gallery-tile:nth-child(1), .gallery-tile:nth-child(2), .gallery-tile:nth-child(3) { grid-column: span 2; }
  }

  @media (max-width: 768px) {
    .container { padding: 0 24px; }
    .header { padding: 24px 0; }
    .burger { display: flex; }
    .nav-menu {
      position: fixed; inset: 0 0 0 auto;
      height: 100vh; width: 100%; max-width: 320px;
      background: #110c0a;
      flex-direction: column; justify-content: center;
      align-items: flex-start; gap: 40px; padding: 0 48px;
      transform: translateX(100%);
      transition: transform 0.6s var(--transition);
      box-shadow: -10px 0 40px rgba(0,0,0,0.3);
    }
    .nav-menu.active { transform: translateX(0); }
    .section { padding: 100px 0; }
    .service-row { grid-template-columns: 1fr; gap: 16px; padding: 32px 0; }
    .service-mark { font-size: 2.8rem; }
    .whatsapp-btn { margin-left: 0; margin-top: 16px; display: block; text-align: center; }
    .instagram-btn { display: block; text-align: center; }
    .gallery-grid{grid-template-columns:repeat(2,1fr); grid-auto-rows:150px;}
    .gallery-tile,
    .gallery-tile:nth-child(1),
    .gallery-tile:nth-child(2),
    .gallery-tile:nth-child(3),
    .gallery-tile:nth-child(4),
    .gallery-tile:nth-child(5),
    .gallery-tile:nth-child(6){grid-column:span 1; grid-row:span 1; padding: 20px;}
    .form-row{grid-template-columns:1fr;}
    .contact-form { padding: 32px 24px; }
  }
</style>
</head>
<body>

<!-- ANNOUNCEMENT BAR -->
<div class="announce-bar">
  Elevate your look. <strong>Private suites available</strong> for beauty professionals.
</div>

<!-- HEADER -->
<header class="header" id="siteHeader">
  <div class="container nav-container">
    <a href="#home" class="logo hover-shake">
      Madina Beauté
      <span>Luxury Studio</span>
    </a>
    <button class="burger" id="burgerBtn" aria-label="Toggle menu">
      <span></span>
      <span></span>
      <span></span>
    </button>
    <ul class="nav-menu" id="navMenu">
      <li><a href="#home" class="nav-link hover-shake">Home</a></li>
      <li><a href="#services" class="nav-link hover-shake">Services</a></li>
      <li><a href="#concept" class="nav-link hover-shake">About</a></li>
      <li><a href="#contacts" class="nav-link hover-shake">Contact</a></li>
    </ul>
  </div>
</header>

<!-- HERO -->
<section class="hero" id="home">
  <div class="container">
    <div class="hero-content">
      <p class="hero-subtitle">Luxury Beauty Salon &amp; Suites — Las Vegas</p>
      <h1 class="hero-title">Your Beauty Journey <i>Starts Here.</i></h1>
      <p class="hero-desc">
        Step into a studio where premium treatments, personalized care, and
        timeless elegance come together — helping you look, and feel, entirely
        your own kind of radiant.
      </p>
      <a href="#contacts" class="btn hover-shake">Book Your Appointment</a>
    </div>
  </div>
  <div class="scroll-cue"><span class="line"></span>Scroll</div>
</section>

<!-- MARQUEE -->
<div class="marquee">
  <div class="marquee-track" id="marqueeTrack">
    <span>Skin Care</span><span>Permanent Makeup</span><span>Lash Extensions</span><span>Nail Services</span>
    <span>Skin Care</span><span>Permanent Makeup</span><span>Lash Extensions</span><span>Nail Services</span>
  </div>
</div>

<!-- SERVICES -->
<section class="section" id="services">
  <div class="container">
    <div class="section-head reveal">
      <span class="eyebrow">What We Do</span>
      <h2 class="section-title">Four crafts, one standard of care.</h2>
    </div>

    <div class="filter-bar reveal" id="filterBar">
      <button class="filter-btn active hover-shake" data-filter="all">All</button>
      <button class="filter-btn hover-shake" data-filter="skin">Skin</button>
      <button class="filter-btn hover-shake" data-filter="pmu">PMU</button>
      <button class="filter-btn hover-shake" data-filter="lashes">Lashes</button>
      <button class="filter-btn hover-shake" data-filter="nails">Nails</button>
    </div>

    <div class="service-list" id="serviceList">
      <div class="service-row show reveal hover-shake" data-cat="skin">
        <div class="service-mark">S</div>
        <h3 class="card-title">Signature Facial</h3>
        <p class="card-desc">A custom facial tailored to your skin's needs — cleansing, exfoliation and a deep hydration finish.</p>
      </div>
      <div class="service-row show reveal hover-shake" data-cat="skin">
        <div class="service-mark">S</div>
        <h3 class="card-title">Deep Cleansing Facial</h3>
        <p class="card-desc">A thorough pore-clearing treatment that leaves skin balanced, clean and visibly refreshed.</p>
      </div>
      <div class="service-row show reveal hover-shake" data-cat="skin">
        <div class="service-mark">S</div>
        <h3 class="card-title">Chemical Peel</h3>
        <p class="card-desc">Professional-grade peels that resurface tone and texture for a brighter, more even complexion.</p>
      </div>

      <div class="service-row show reveal hover-shake" data-cat="pmu">
        <div class="service-mark">P</div>
        <h3 class="card-title">Powder Brows</h3>
        <p class="card-desc">Soft, shaded brows with a natural powder finish that lasts through every routine.</p>
      </div>
      <div class="service-row show reveal hover-shake" data-cat="pmu">
        <div class="service-mark">P</div>
        <h3 class="card-title">Lip Blush</h3>
        <p class="card-desc">A subtle wash of color that defines and evens the lips — effortless, every day.</p>
      </div>
      <div class="service-row show reveal hover-shake" data-cat="pmu">
        <div class="service-mark">P</div>
        <h3 class="card-title">Eyeliner Tattoo</h3>
        <p class="card-desc">Precise, long-wearing definition along the lash line — wake up already done.</p>
      </div>

      <div class="service-row show reveal hover-shake" data-cat="lashes">
        <div class="service-mark">L</div>
        <h3 class="card-title">Classic Lash Set</h3>
        <p class="card-desc">One extension per natural lash for a clean, mascara-like finish.</p>
      </div>
      <div class="service-row show reveal hover-shake" data-cat="lashes">
        <div class="service-mark">L</div>
        <h3 class="card-title">Volume Lashes</h3>
        <p class="card-desc">Handcrafted fans built for fuller, darker, more dramatic lash lines.</p>
      </div>
      <div class="service-row show reveal hover-shake" data-cat="lashes">
        <div class="service-mark">L</div>
        <h3 class="card-title">Lash Lift &amp; Tint</h3>
        <p class="card-desc">Lifts and tints your natural lashes for a curled, defined look with zero extensions.</p>
      </div>

      <div class="service-row show reveal hover-shake" data-cat="nails">
        <div class="service-mark">N</div>
        <h3 class="card-title">Luxury Manicure</h3>
        <p class="card-desc">Premium products and impeccable sanitation for perfectly finished hands.</p>
      </div>
      <div class="service-row show reveal hover-shake" data-cat="nails">
        <div class="service-mark">N</div>
        <h3 class="card-title">Gel Pedicure</h3>
        <p class="card-desc">A long-wear, high-shine finish paired with a relaxing foot treatment.</p>
      </div>
      <div class="service-row show reveal hover-shake" data-cat="nails">
        <div class="service-mark">N</div>
        <h3 class="card-title">Nail Art &amp; Design</h3>
        <p class="card-desc">Custom detailing and design work for nails that make a statement.</p>
      </div>
    </div>
  </div>
</section>

<!-- BEYOND THE CHAIR -->
<section class="section" style="padding-top:0;">
  <div class="container">
    <div class="section-head reveal">
      <span class="eyebrow">Beyond the Chair</span>
      <h2 class="section-title">More than a salon.</h2>
    </div>
    <div class="beyond-grid reveal">
      <div class="beyond-item hover-shake">
        <h3>Bridal &amp; Event Glam</h3>
        <p>Lashes, brows, nails and glowing skin for your wedding day or big night out — all done in one visit, all in one place.</p>
      </div>
      <div class="beyond-item hover-shake">
        <h3>Private Salon Suites</h3>
        <p>Luxury private suites for beauty professionals. Build your own business inside Madina Beauté Studio — inquire about availability.</p>
      </div>
      <div class="beyond-item hover-shake">
        <h3>Gift Cards</h3>
        <p>Give someone a day of self-care. Available for any amount or treatment — beautifully packaged and ready to make someone's day.</p>
      </div>
    </div>
  </div>
</section>

<!-- GALLERY -->
<section class="section" style="padding-top:0;">
  <div class="container">
    <div class="section-head reveal">
      <span class="eyebrow">Our Space</span>
      <h2 class="section-title">Studio Gallery</h2>
    </div>
    <div class="gallery-grid reveal">
      <div class="gallery-tile"><span class="gallery-label">Main Salon Area</span></div>
      <div class="gallery-tile"><span class="gallery-label">Skin Care Suite</span></div>
      <div class="gallery-tile"><span class="gallery-label">Nail Lounge</span></div>
      <div class="gallery-tile"><span class="gallery-label">PMU Room</span></div>
      <div class="gallery-tile"><span class="gallery-label">Lash Station</span></div>
      <div class="gallery-tile"><span class="gallery-label">Private Suites</span></div>
    </div>
  </div>
</section>

<!-- ABOUT -->
<section class="section concept-bg" id="concept">
  <div class="container">
    <div class="section-head reveal">
      <span class="eyebrow">Why Madina Beauté</span>
      <h2 class="section-title">A quieter kind of luxury.</h2>
    </div>
    <div class="concept-grid">
      <div class="concept-item reveal hover-shake">
        <h3>Beauty with Purpose</h3>
        <p>
          We believe beauty begins with healthy skin, expert care, and
          personalized treatments tailored to your unique needs.
        </p>
      </div>

      <div class="concept-item reveal hover-shake">
        <h3>Luxury Experience</h3>
        <p>
          Enjoy a relaxing atmosphere, premium products, and exceptional service
          designed to make every visit unforgettable.
        </p>
      </div>

      <div class="concept-item reveal hover-shake">
        <h3>Confidence Every Day</h3>
        <p>
          Whether it's glowing skin, flawless brows, beautiful lashes, or
          perfect nails, we're here to help you feel confident every day.
        </p>
      </div>
    </div>
  </div>
</section>

<!-- CONTACT & FORM -->
<section class="section" id="contacts">
  <div class="container">
    <div class="section-head reveal">
      <span class="eyebrow">Visit Us</span>
      <h2 class="section-title">Contact &amp; Location</h2>
    </div>
    <div class="contacts-wrapper">
      <div class="contacts-info reveal">
        <div class="contact-item">
          <h4>Location</h4>
          <p>Las Vegas, Nevada</p>
        </div>

        <div class="contact-item">
          <h4>Phone</h4>
          <p><a href="tel:+17253125555">+1 (725) 312-5555</a></p>
        </div>

        <div class="contact-item">
          <h4>Instagram</h4>
          <p style="font-family:var(--font-base); font-weight:400; font-size:1.05rem;">
            @madina.beaute.studio__
          </p>
        </div>

        <div style="margin-top: 32px;">
          <a
            href="https://instagram.com/madina.beaute.studio__"
            target="_blank"
            class="btn instagram-btn hover-shake"
          >
            Instagram
          </a>
          <a
            href="https://wa.me/17253125555"
            target="_blank"
            class="btn whatsapp-btn hover-shake"
          >
            WhatsApp
          </a>
        </div>
        
        <div class="map-box reveal">
          <iframe
            src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d3221.2772294457336!2d-115.1936995!3d36.1598079!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x80c8c14127e534ef%3A0xeabdaaeab406b51a!2zTWFkw61uYSBCZWF1dMOpIFN0dWTDrW8!5e0!3m2!1sru!2sus!4v1783367837745!5m2!1sru!2sus"
            width="100%"
            height="320"
            style="border:0;"
            allowfullscreen
            loading="lazy"
          >
          </iframe>
        </div>
      </div>

      <form class="contact-form reveal" onsubmit="event.preventDefault();">
        <div class="form-row">
          <div class="form-field">
            <label for="formName">Full Name</label>
            <input type="text" id="formName" placeholder="Your name" required />
          </div>
          <div class="form-field">
            <label for="formPhone">Phone Number</label>
            <input type="tel" id="formPhone" placeholder="Your phone" required />
          </div>
        </div>
        <div class="form-field">
          <label for="formService">Requested Service</label>
          <select id="formService">
            <option value="skin">Skin Care Treatments</option>
            <option value="pmu">Permanent Makeup (PMU)</option>
            <option value="lashes">Lash Extensions / Lift</option>
            <option value="nails">Luxury Nail Services</option>
            <option value="suites">Salon Suite Inquiry</option>
          </select>
        </div>
        <div class="form-field">
          <label for="formMessage">Notes / Preferred Date</label>
          <textarea id="formMessage" placeholder="Tell us about your preferred time or custom needs..."></textarea>
        </div>
        <button type="submit" class="btn form-submit hover-shake">Send Request</button>
        <span class="form-note">✦ We typically reply within 1-2 business hours.</span>
      </form>
    </div>
  </div>
</section>

<!-- FOOTER -->
<footer class="footer">
  <div class="container">
    <div class="footer-logo">MADINA BEAUTÉ</div>
    <div class="footer-tagline">
      Skin Care • Permanent Makeup • Lash Extensions • Nail Services
    </div>
    <p class="copyright">
      &copy; 2026 Madina Beauté Studio. All Rights Reserved.
    </p>
  </div>
</footer>

<script>
document.addEventListener('DOMContentLoaded', function () {
  const burgerBtn = document.getElementById('burgerBtn');
  const navMenu = document.getElementById('navMenu');
  const navLinks = document.querySelectorAll('.nav-link');
  const header = document.getElementById('siteHeader');

  if (burgerBtn && navMenu) {
    burgerBtn.addEventListener('click', () => {
      burgerBtn.classList.toggle('active');
      navMenu.classList.toggle('active');
    });
  }

  navLinks.forEach(link => {
    link.addEventListener('click', () => {
      if (burgerBtn) burgerBtn.classList.remove('active');
      if (navMenu) navMenu.classList.remove('active');
    });
  });

  if (header) {
    window.addEventListener('scroll', () => {
      header.classList.toggle('scrolled', window.scrollY > 40);
    });
  }

  const filterBtns = document.querySelectorAll('.filter-btn');
  const serviceRows = document.querySelectorAll('.service-row');
  filterBtns.forEach(btn => {
    btn.addEventListener('click', () => {
      filterBtns.forEach(b => b.classList.remove('active'));
      btn.classList.add('active');
      const filter = btn.dataset.filter;
      serviceRows.forEach(row => {
        const match = filter === 'all' || row.dataset.cat === filter;
        row.classList.toggle('show', match);
      });
    });
  });

  document.querySelectorAll('a[href^="#"]').forEach(link => {
    link.addEventListener('click', (e) => {
      const id = link.getAttribute('href').slice(1);
      const target = document.getElementById(id);
      if (target) {
        e.preventDefault();
        target.scrollIntoView({ behavior: 'smooth', block: 'start' });
      }
    });
  });
});
</script>
<script>
  document.addEventListener('DOMContentLoaded', function () {
    const filterBtns = document.querySelectorAll('.filter-btn');
    const serviceRows = document.querySelectorAll('.service-row');

    filterBtns.forEach(btn => {
      btn.addEventListener('click', () => {
        // Убираем активный класс у всех кнопок и добавляем нажатой
        filterBtns.forEach(b => b.classList.remove('active'));
        btn.classList.add('active');
        
        const filter = btn.dataset.filter;
        
        // Показываем или скрываем строки услуг в зависимости от фильтра
        serviceRows.forEach(row => {
          
          const match = filter === 'all' || row.dataset.cat === filter;
          row.classList.toggle('show', match);
        });
      });
    });
  });
</script>

</body>
</html> 